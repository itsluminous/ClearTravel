package com.itsluminous.cleartravel.core.notifications

/**
 * Stable notification channel ids. Channel NAMES/descriptions are user-visible and
 * therefore live in string resources at registration time — only the ids are constants.
 */
object NotificationChannels {
    const val CHANNEL_TRAINS = "trains"
    const val CHANNEL_FLIGHTS = "flights"
    const val CHANNEL_REMINDERS = "reminders"
}
